import $ from 'jquery';

// create/display sections divided by color to allow side scrolling when buttons are held/pressed (make it go both ways to set limits)

const BarSlide = () => {
    return (
        <div className=''></div>
    )
}

export default BarSlide;